---
/*
 * Home — the drafting sheet's title block + plotted hero.
 *
 * Hero THESIS: the live SIP plotted as a growth curve on graph paper (the most
 * characteristic artifact of a quantitative India-finance world — money
 * compounding, drawn and dimensioned like engineering print). Below it, the
 * catalogue reads as a bill-of-materials: 28 calculators across 4 sheets, each
 * a numbered part with a live/soon status stamp.
 */
import InlineSIPCalculator from '~/components/InlineSIPCalculator.tsx'
import BaseLayout from '~/layouts/BaseLayout.astro'
import { CALCULATORS, LEDGERS } from '~/lib/catalogue'
import { SITE_CONFIG } from '~/lib/siteConfig'

const liveCount = CALCULATORS.filter((c) => c.live).length

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: `${SITE_CONFIG.name} — oriz`,
  url: SITE_CONFIG.origin,
  description: SITE_CONFIG.description,
  publisher: { '@type': 'Person', name: 'Chirag Singhal', url: 'https://oriz.in' },
}
---

<BaseLayout jsonLd={jsonLd}>
  {/* ── Hero — title block + plotted curve ── */}
  <section class="spine hero" aria-label="Engineering-Print Ledger">
    <p class="hero-eyebrow mono">Sheet 00 · finance.oriz.in · India personal-finance</p>
    <h1 class="hero-h">The math,<br />plotted.</h1>
    <p class="hero-sub">
      <span class="num">{CALCULATORS.length}</span> personal-finance calculators for India —
      SIP, EMI, FIRE, FD, PPF, NPS, HRA, TDS, take-home. Each one draws the result on graph
      paper as you type. No sign-up, no advisor pitch, no numbers leaving your device.
    </p>

    <InlineSIPCalculator client:idle />
  </section>

  <hr class="hairline" />

  {/* ── Index — the bill-of-materials ── */}
  <section id="index" class="spine reg" aria-label="Calculator index">
    <div class="reg-head">
      <h2 class="reg-h mono">Bill of materials</h2>
      <p class="reg-count mono">
        <span class="num">{liveCount}</span>/<span class="num">{CALCULATORS.length}</span> parts drawn
      </p>
    </div>

    {LEDGERS.map((ledger, li) => (
      <div class="reg-ledger">
        <h3 class="reg-ledger-h">
          <span class="reg-ledger-no mono">SH-{String(li + 1).padStart(2, '0')}</span>
          {ledger.label}
        </h3>
        <ul class="reg-keys">
          {CALCULATORS.filter((c) => c.ledger === ledger.key).map((c, ci) => (
            <li>
              <a class="key-row" href={`/calculators/${c.slug}/`}>
                <span class="key-no mono">{String(li + 1)}.{String(ci + 1).padStart(2, '0')}</span>
                <span class="key-name">{c.name}</span>
                <span class="key-brief">{c.brief}</span>
                <span class={`key-plate mono ${c.live ? 'is-live' : ''}`}>{c.live ? 'live' : 'soon'}</span>
              </a>
            </li>
          ))}
        </ul>
      </div>
    ))}
  </section>

  <hr class="hairline" />

  {/* ── Colophon ── */}
  <section class="spine note" aria-label="How this works">
    <h2 class="note-h mono">Drawing notes</h2>
    <div class="note-prose">
      <p>
        Every calculator computes in your browser — the formula, the year-by-year
        breakdown, the plotted curve, all of it client-side. Inputs never leave your
        device. Sign-in is optional and only unlocks saving a scenario to re-open on
        another device; it works across every oriz site.
      </p>
      <p>
        A couple of pages carry a display ad to cover the domain renewal.
        Nothing is gated. <a href="/legal/disclaimer/">Read the SEBI disclaimer →</a>
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    padding-block: clamp(2.5rem, 7vw, 4rem) clamp(2rem, 5vw, 3rem);
  }
  .hero-eyebrow {
    font-size: 10px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--ink-mute);
    margin: 0 0 1rem;
  }
  .hero-h {
    font-family: var(--font-display);
    font-weight: 700;
    text-transform: uppercase;
    font-size: clamp(2.6rem, 9vw, 5.5rem);
    line-height: 0.9;
    letter-spacing: 0.01em;
    color: var(--ink);
    margin: 0 0 1.25rem;
  }
  .hero-sub {
    font-size: 1.0625rem;
    line-height: 1.6;
    color: var(--ink-mute);
    margin: 0 0 2.25rem;
    max-width: 60ch;
  }

  /* ── Bill of materials ── */
  .reg { padding-block: clamp(2rem, 5vw, 3rem); }
  .reg-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    border-bottom: 2px solid var(--credit);
    padding-bottom: 0.6rem;
  }
  .reg-h, .reg-count {
    font-family: var(--font-mono);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--ink-mute);
    margin: 0;
  }
  .reg-ledger { margin-top: 2rem; }
  .reg-ledger-h {
    font-family: var(--font-display);
    font-weight: 700;
    text-transform: uppercase;
    font-size: clamp(1.25rem, 2.4vw, 1.6rem);
    color: var(--ink);
    letter-spacing: 0.01em;
    margin: 0 0 0.75rem;
    display: flex;
    align-items: baseline;
    gap: 0.7rem;
  }
  .reg-ledger-no {
    font-size: 11px;
    color: var(--credit);
    letter-spacing: 0.12em;
    font-weight: 500;
  }
  .reg-keys {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.5rem;
  }
  @media (min-width: 760px) {
    .reg-keys { grid-template-columns: repeat(2, 1fr); gap: 0.5rem 1.5rem; }
  }
  .key-row {
    display: grid;
    grid-template-columns: max-content 1fr max-content;
    grid-template-areas: 'no name plate' 'no brief plate';
    align-items: center;
    gap: 0 0.75rem;
    padding: 0.55rem 0.75rem;
    background: var(--tape);
    border: 1px solid var(--rule);
    border-left: 3px solid var(--rule);
  }
  .key-row:hover {
    border-left-color: var(--credit);
    background: color-mix(in oklab, var(--credit) 6%, var(--tape));
  }
  .key-row:focus-visible { outline: 2px solid var(--credit); outline-offset: 2px; }
  .key-no {
    grid-area: no;
    font-size: 11px;
    color: var(--ink-mute);
    letter-spacing: 0.04em;
    align-self: center;
  }
  .key-name { grid-area: name; font-weight: 600; font-size: 15px; color: var(--ink); }
  .key-brief { grid-area: brief; font-size: 12px; color: var(--ink-mute); line-height: 1.35; }
  .key-plate {
    grid-area: plate;
    font-size: 9px;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: var(--ink-mute);
    border: 1px solid var(--rule);
    padding: 0.15rem 0.4rem;
  }
  .key-plate.is-live { color: var(--credit); border-color: color-mix(in oklab, var(--credit) 50%, var(--rule)); }

  /* ── Drawing notes ── */
  .note { padding-block: clamp(2rem, 5vw, 3rem) 2rem; }
  .note-h {
    font-family: var(--font-mono);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--ink-mute);
    margin: 0 0 1rem;
  }
  .note-prose p { color: var(--ink); line-height: 1.7; margin: 0 0 1rem; max-width: 64ch; }
  .note-prose a {
    color: var(--credit);
    text-decoration: underline;
    text-decoration-color: color-mix(in oklab, var(--credit) 50%, transparent);
    text-underline-offset: 3px;
  }
  .note-prose a:hover { text-decoration-color: var(--credit); }
</style>
